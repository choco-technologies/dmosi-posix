name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Configure CMake
        run: cmake -B build -DDMOSI_POSIX_BUILD_TESTS=ON

      - name: Build
        run: cmake --build build

      - name: Verify library was created
        run: |
          test -f build/libdmosi_posix.a
          echo "✓ libdmosi_posix.a created successfully"

      - name: Run tests
        run: ctest --test-dir build --output-on-failure
